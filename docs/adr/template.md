# ADR-NNNN — Title

**Status:** Proposed | Accepted | Superseded by [ADR-NNNN](nnnn-title.md) | Deprecated
**Date:** YYYY-MM-DD

## Context

Why we had to make a decision. The problem, the constraint, the pain that
motivated the choice. Include concrete evidence when available (file paths,
line counts, past incidents, failing tests).

## Decision

What we decided. One or two paragraphs, written in active voice. Prefer
precision over hedging — the ADR is the written-down outcome, not the
meeting transcript.

## Consequences

Positive and negative trade-offs. What this unlocks, what it forecloses,
what becomes harder. Use sub-bullets with `(+)` and `(−)` markers so the
asymmetry is visible at a glance.

## Alternatives considered

Short list of the other paths and the one-line reason each was rejected.
Skip this section only when there was genuinely no alternative.

## References

- Code pointers (`src/.../File.cs`)
- Related ADRs
- Related issues / PRs / commits
- External links (blog posts, RFCs) when they informed the decision
