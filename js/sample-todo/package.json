{
  "name": "sample-todo",
  "type": "module",
  "private": true,
  "sideEffects": false,
  "imports": {
    "#/*": "./src/*"
  },
  "dependencies": {
    "@meta-sharp/runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript/native-preview": "^7.0.0-dev.20260225.1"
  },
  "scripts": {
    "generate": "dotnet run --project ../../MetaSharp.Compiler/MetaSharp.Compiler.csproj -- -p ../../SampleTodo/SampleTodo.csproj -o ./src --clean --time",
    "build": "tsgo -b .",
    "test": "bun test"
  }
}
