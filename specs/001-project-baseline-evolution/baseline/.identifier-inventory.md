# Stable Identifier Inventory (working note)

Every stable identifier carried from `old-spec/` that MUST keep resolving after migration (CR-006,
SC-005). Transient working artifact.

## Functional Requirements — `FR-001`–`FR-048`

- `FR-001`–`FR-004` Discovery & selection
- `FR-005`–`FR-014` Semantic transformation (enums, records, classes, interfaces, nullability, async,
  exceptions, operators, pattern matching, expressions)
- `FR-015`–`FR-018` Types & collections (BCL types, collections, LINQ, inline wrappers)
- `FR-019`–`FR-026` Attribute-driven customization
- `FR-027`–`FR-031` Output (.ts files, imports, barrels, package.json, cross-package)
- `FR-032`–`FR-034` Serialization & validation
- `FR-035`–`FR-037`, `FR-046`, `FR-048` Integration (build, CLI, real projects, build-only deps, assets)
- `FR-038`–`FR-040` Diagnostics
- `FR-041`–`FR-045`, `FR-047` Advanced emission (delegates/events, overloads, entry points, collection
  expressions, cyclic detection, namespace-derived subpaths)

> Note: the legacy FR set is numbered `FR-001`–`FR-048` but is non-contiguous in places; the migration
> preserves whatever IDs exist in `old-spec/04-functional-requirements.md`, mapped to matrix rows.

## Non-Functional Requirements — `NFR-001`–`NFR-020`

Output quality, semantic reliability, integration/adoptability, maintainability, observability,
performance, compatibility/stability.

## Diagnostics — `MS0001`–`MS0025`

Full list in [diagnostic-catalog.md](./diagnostic-catalog.md). (Legacy stopped at `MS0022`.)

## Attributes (`Metano.Annotations`, 27)

Full list in [attribute-catalog.md](./attribute-catalog.md). + 3 TS-backend-specific
(`[Discriminator]`, `[External]`, `[StrictUnionGuard]`).

## ADRs — `ADR-0001`–`ADR-0024`

Home unchanged: `docs/adr/`. Cross-reference retained.
